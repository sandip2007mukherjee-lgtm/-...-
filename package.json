{
  "name": "emotion-note-functions",
  "description": "AI backend for Emotion Note",
  "scripts": {
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-functions": "^7.0.0"
  },
  "private": true
}
